server {
    listen 8001;

    location / {
        # When not using docker compose replace with your server's IP address and port
        proxy_pass http://soundcork:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;

        # Hide the lowercase etag from the upstream (FastAPI)
        proxy_hide_header etag;

        # Re-add it with the specific Bose-friendly casing
        # $upstream_http_etag captures the value from the hidden header
        add_header ETag $upstream_http_etag;
    }
}
